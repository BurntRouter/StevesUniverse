package com.steveuniverse.items;

import net.minecraft.entity.Entity;
import net.minecraft.entity.player.EntityPlayer;
import net.minecraft.item.Item;
import net.minecraft.item.ItemStack;
import net.minecraft.world.World;
import net.minecraftforge.client.event.sound.PlaySoundEvent;

public class stevenU extends Item{
/**	public void onItemRightClick(ItemStack itemStack, World world, Entity entity, int par4, boolean par5)
	{
	EntityPlayer player = (EntityPlayer) entity;
	ItemStack heldItem = player.getCurrentEquippedItem();
	for(int i = 0; i <= player.inventory.getSizeInventory(); i++)
	{
	if(heldItem != null && heldItem.getItem() instanceof GiantW)
	{
		world.playSoundAtEntity(player, "GiantW", 1f, 1f);
	}
	else
	{
		if(heldItem != null && heldItem.getItem() instanceof JamS)
		{	
			world.playSoundAtEntity(player, "JamS", 1f, 1f);
		}
		else
		{
			if(heldItem != null && heldItem.getItem() instanceof ForH)
			{	
				world.playSoundAtEntity(player, "ForH", 1f, 1f);	
			}
			else
			{
				if(heldItem != null && heldItem.getItem() instanceof FullD)
				{	
					world.playSoundAtEntity(player, "FullD", 1f, 1f);
				}
				else
				{
					if(heldItem != null && heldItem.getItem() instanceof StrongerTY)
					{	
						world.playSoundAtEntity(player, "StrongerTY", 1f, 1f);
					}
					else
					{
						if(heldItem != null && heldItem.getItem() instanceof WhatCID)
						{	
							world.playSoundAtEntity(player, "WhatCID", 1f, 1f);
						}
						
										
										}
									}
								}
							}
						}
					}
				} */
		


	}

